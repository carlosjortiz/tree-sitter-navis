==================
request with name and GET
==================

### Login
GET https://api.example.com/users

---

(source_file
  (request
    (request_separator
      name: (value))
    method: (method)
    url: (target_url)))

==================
request without name
==================

###
GET https://api.example.com/users

---

(source_file
  (request
    (request_separator)
    method: (method)
    url: (target_url)))

==================
common HTTP methods
==================

### Get
GET /users
### Create
POST /users
### Update
PUT /users
### Delete
DELETE /users
### Patch
PATCH /users

---

(source_file
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url))
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url))
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url))
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url))
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url)))

==================
GraphQL and WebSocket methods
==================

### Query
GRAPHQL {{base}}/graphql
### Stream
WEBSOCKET {{base}}/ws

---

(source_file
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url
      (placeholder name: (identifier))))
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url
      (placeholder name: (identifier)))))

==================
URL with placeholders
==================

### Get user
GET {{api.base_url}}/users/{{id}}

---

(source_file
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url
      (placeholder
        qualifier: (qualifier)
        name: (identifier))
      (placeholder
        name: (identifier)))))

==================
URL with text and placeholder mix
==================

### Create
POST https://{{api.host}}:8080/v1/users

---

(source_file
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url
      (placeholder
        qualifier: (qualifier)
        name: (identifier)))))

==================
request line with HTTP version
==================

### Versioned
GET /a/b HTTP/1.1

---

(source_file
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url)
    version: (http_version)))

==================
multi-line URL with continuation
==================

### Long
GET {{base}}/very/long/path
  ?foo=1&bar=2

---

(source_file
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url
      (placeholder name: (identifier)))))

==================
request name with placeholder
==================

### User {{request.userId}}
GET {{base}}/users

---

(source_file
  (request
    (request_separator
      name: (value
        (placeholder
          qualifier: (qualifier)
          name: (identifier))))
    method: (method)
    url: (target_url
      (placeholder name: (identifier)))))

==================
multiple requests with endpoint vars and comments
==================

@base = https://api.example.com

# Authentication endpoints

### Login
POST {{base}}/auth/login

### Get current user
GET {{base}}/me

---

(source_file
  (endpoint_var
    name: (identifier)
    value: (value))
  (comment)
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url
      (placeholder name: (identifier))))
  (request
    (request_separator name: (value))
    method: (method)
    url: (target_url
      (placeholder name: (identifier)))))
